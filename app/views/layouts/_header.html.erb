 <div class="navbar navbar-fluid-top">
      <div class="navbar-inner">
        <div class="container-fluid">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <a class="brand" href="<%= root_path %>">Great Heights Inventory Tracker</a>
          <div class="container-fluid nav-collapse">
          <% if user_signed_in? %>
            <ul class="nav">
              <!-- <li><%= link_to "Link1", "/path1", :class => "btn btn-primary" %></li> -->
              <!-- <li><%= link_to "Link2", "/path2", :class => "btn btn-primary" %></li> -->
              <!-- <li><%= link_to "Link3", "/path3", :class => "btn btn-primary" %></li> -->
              <li>
	              <% if user_signed_in? %>
		              <%= link_to('Logout', destroy_user_session_path, :method => :get, :class => "btn btn-primary") %>
	              <% else %>
	              	  <%= link_to('Login', new_user_session_path, :class => "btn btn-primary")  %>  
	              <% end %>
              </li>
            </ul>
          <% end %>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>